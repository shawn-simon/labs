//sort an array so all anagrams are next to each other
// didn't do this one, too easy
'use strict';
let items = ['test', 'a', 'b', 'estt', 'jar', 'raw', 'raj'];

function sort(items) {
  let map = {};
  items.forEach((val, i) => {

  })
}

console.log(sort(items))
